# Генератор кадров путей графа

Автоматическая генерация визуализации всех путей из вершины A в вершину H.

## 📋 Описание

Этот проект автоматически генерирует PNG-кадры для каждого возможного пути в ориентированном графе. Каждый кадр показывает:
- **Яркую подсветку** текущего пути (красные рёбра, оранжевые вершины)
- **Бледное отображение** остальных рёбер и вершин графа
- **Заголовок** с описанием пути

## 🎯 Структура графа

```
A → B → D → H
A → B → E → H
A → C → F → G → H
```

Граф содержит:
- 8 вершин: A, B, C, D, E, F, G, H
- 9 рёбер (направленных)
- 3 различных пути из A в H

## 🚀 Запуск

### Генерация кадров

```bash
# Активировать виртуальное окружение и запустить скрипт
./venv/bin/python generate_path_frames.py
```

Скрипт создаст файлы:
- `frame_01.png` - Путь A → B → D → H
- `frame_02.png` - Путь A → B → E → H
- `frame_03.png` - Путь A → C → F → G → H

### Просмотр кадров

Откройте файл `viewer.html` в браузере для интерактивного просмотра всех путей.

Возможности:
- ⬅️➡️ Навигация между кадрами
- ▶️ Автоматический показ слайдов
- 🖼️ Миниатюры для быстрого перехода
- ⌨️ Управление клавиатурой (стрелки, пробел)

## 📦 Зависимости

- Python 3
- networkx - для работы с графами
- matplotlib - для визуализации

Все зависимости установлены в виртуальном окружении `venv/`.

## 📁 Файлы проекта

- `generate_path_frames.py` - основной скрипт генерации
- `viewer.html` - интерактивный просмотрщик кадров
- `experiment.md` - исходный код эксперимента
- `frame_*.png` - сгенерированные кадры
- `venv/` - виртуальное окружение Python

## 🎨 Особенности визуализации

- **Размер кадра**: 10x6 дюймов
- **Разрешение**: 150 DPI
- **Цвета пути**: оранжевые вершины, красные рёбра (ширина 3)
- **Цвета фона**: светло-серые вершины, бледно-серые рёбра (прозрачность 40%)
- **Направленные рёбра**: со стрелками для указания направления

## 💡 Использование как шаблона

Чтобы использовать этот код для других графов:

1. Измените список рёбер в переменной `edges`
2. Настройте координаты вершин в словаре `pos`
3. Укажите начальную и конечную вершины в `nx.all_simple_paths()`
4. Запустите скрипт

Пример:
```python
edges = [('A', 'B'), ('B', 'C'), ...]
pos = {'A': (0, 0), 'B': (1, 1), ...}
paths = list(nx.all_simple_paths(G, source='A', target='H'))
```

## 📊 Вывод программы

```
Найдено путей из A в H: 3
Пути:
  1. A -> B -> D -> H
  2. A -> B -> E -> H
  3. A -> C -> F -> G -> H
Сохранён кадр: frame_01.png
Сохранён кадр: frame_02.png
Сохранён кадр: frame_03.png

Всего создано кадров: 3
```
