# 🗺️ Задание 4 ОГЭ 2026 - Кратчайший путь

**Визуализация алгоритма поиска кратчайшего пути в графе**

## 📋 Описание задания

Найти длину кратчайшего пути между двумя вершинами в взвешенном графе, где каждое ребро имеет свой вес (расстояние).

### Пример задания

```
Между населёнными пунктами A, B, C, D, E построены дороги,
протяжённость которых (в километрах) приведена в таблице.

    A   B   C   D   E
A   -   2   6   -   -
B   2   -   -   -   -
C   6   -   -   3   8
D   -   -   3   -   5
E   -   -   8   5   -

Определите длину кратчайшего пути между пунктами A и D.
Передвигаться можно только по дорогам.
Каждый пункт можно посетить только один раз.
```

## 🎯 Алгоритм: Динамическое программирование на DAG

### Принцип работы

1. **Начальная вершина** = 0 (расстояние от A до A = 0)
2. **Для каждой следующей вершины:**
   - Расстояние = МИНИМУМ из всех путей через входящие рёбра
   - dist[v] = min(dist[u] + weight(u, v)) для всех рёбер u→v
3. **Конечная вершина** содержит минимальное расстояние

### Формула

```
dist[A] = 0
dist[v] = min(dist[u] + weight(u→v)) для всех u→v
```

## 🎬 Как использовать визуализатор

### Шаг 1: Загрузка графа
1. Загрузите изображение с таблицей расстояний
2. ИЛИ нарисуйте граф с нуля

### Шаг 2: Разметка вершин
1. Выберите режим "Добавить вершину"
2. Кликните на изображении в местах вершин
3. Вершины автоматически получат названия A, B, C, D, E...

### Шаг 3: Соединение рёбрами
1. Выберите режим "Добавить ребро"
2. Кликните на первую вершину
3. Кликните на вторую вершину
4. Введите вес ребра (расстояние)

### Шаг 4: Запуск алгоритма
1. Нажмите "Построить граф"
2. Нажмите "Запустить поиск" для автоматической анимации
3. ИЛИ используйте "Следующий шаг" для пошагового просмотра

## 📊 Визуализация

### Цветовая схема вершин
- **Серая** (#f0f0f0) - неактивная вершина (ещё не обработана)
- **Жёлтая** (#fff3cd) - текущая вершина (обрабатывается сейчас)
- **Зелёная** (#d4edda) - завершённая вершина (расстояние вычислено)

### Рёбра
- **Светло-серые** (#e0e0e0) - неактивные рёбра
- **Тёмно-серые** (#999) - активные рёбра (используются в вычислении)
- **Веса в кружках** - показывают расстояние на ребре

### Панель информации
- **Таблица расстояний** - минимальные расстояния до каждой вершины
- **Текущее вычисление** - формула для текущей вершины
- **Алгоритм** - описание метода

## 🔍 Пример решения

### Граф
```
A --2--> B
|        |
6        ?
|        |
v        v
C --3--> D <--5-- E
|        ^
8        |
+--------+
```

### Вычисления
```
dist[A] = 0                          # Начало
dist[B] = min(0 + 2) = 2             # A→B
dist[C] = min(0 + 6) = 6             # A→C
dist[D] = min(2 + ?, 6 + 3) = 9      # B→D или C→D
dist[E] = min(6 + 8) = 14            # C→E
```

**Ответ:** Кратчайший путь A→D = 9 км (через A→C→D)

## 📝 Особенности алгоритма

### Топологическая сортировка
- Граф должен быть ориентированным ациклическим (DAG)
- Вершины обрабатываются в топологическом порядке
- Гарантирует, что все входящие рёбра уже обработаны

### Отличия от алгоритма Дейкстры
- **ДП на DAG**: работает только на ациклических графах, но быстрее O(V+E)
- **Дейкстра**: работает на любых графах с неотрицательными весами O((V+E)log V)

### Преимущества метода
- ✅ Линейная сложность O(V+E)
- ✅ Простая реализация
- ✅ Наглядная визуализация
- ✅ Легко понять принцип работы

## 🎓 Теория

### DAG (Directed Acyclic Graph)
**Ориентированный ациклический граф** - граф, в котором:
- Все рёбра имеют направление
- Нет циклов (нельзя вернуться в исходную вершину)

### Динамическое программирование
**Метод оптимизации** основан на:
- Разбиении задачи на подзадачи
- Использовании результатов решённых подзадач
- Построении решения "снизу вверх"

## 💡 Советы для решения на ОГЭ

1. **Нарисуйте граф** из таблицы смежности
2. **Найдите начальную вершину** (часто это A)
3. **Идите по порядку** (A→B→C→D...)
4. **Для каждой вершины** ищите минимум из всех путей
5. **Запишите ответ** из конечной вершины

## 🚀 Технические детали

- **Язык:** HTML5 + JavaScript (Canvas API)
- **Библиотеки:** Нет (vanilla JS)
- **Браузер:** Любой современный браузер
- **Размер:** ~20 KB

## 📚 Ссылки

- [Алгоритмы на графах](https://ru.wikipedia.org/wiki/Алгоритм_на_графах)
- [Динамическое программирование](https://ru.wikipedia.org/wiki/Динамическое_программирование)
- [Задание 4 ОГЭ по информатике](https://kpolyakov.spb.ru/school/oge.htm)
